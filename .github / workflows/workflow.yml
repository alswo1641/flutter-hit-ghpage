name: Flutter Web
on:
  push:
    branches:
      - master
jobs:
  build:
    name: Build Web
    env:
      my_secret: $
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: subosito/flutter-action@v1
        with:
          channel: 'dev'
      - run: flutter config --enable-web
      - run: flutter pub get
      - run: flutter build web --release
        # 1. change path which exist lib/main.dart
        working-directory: ./hit_project
      - run: |
          # 2. change path to [existed lib/main.dart path]/build/web
          cd hit_project/build/web
          git init
           # 3. change your email
          git config --global user.email kny71167@gmail.com
          # 4. change your name
          git config --global user.name alswo1641
          git status
          # 5. change your repository
          git remote add origin https://github.com/alswo1641/hitProject.git
          git checkout -b gh-pages
          git add --all
          git commit -m "update"
          git push origin gh-pages -f